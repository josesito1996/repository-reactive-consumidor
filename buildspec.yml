version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase....
      - mvn clean package
  post_build:
    commands:
      - echo Build completed succesfully
artifacts:
  files:
    - target/repository-reactive.jar
  discard-paths: yes